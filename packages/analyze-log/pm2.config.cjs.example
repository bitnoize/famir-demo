const { env } = require('../../env.config.cjs')

module.exports = {
  apps: [
    {
      name: 'famir-demo-analyze-log',
      script: 'dist/index.js',
      cwd: __dirname,
      env: {
        'NODE_ENV': 'production',
        'LOGGER_APP_NAME': 'famir-demo-analyze-log',
        'LOGGER_LOG_LEVEL': 'debug',
        'LOGGER_TRANSPORT_TARGET': env.LOGGER_TRANSPORT_TARGET,
        'LOGGER_TRANSPORT_OPTIONS': env.LOGGER_TRANSPORT_OPTIONS,
        'DATABASE_CONNECTION_URL': env.DATABASE_CONNECTION_URL,
        //'DATABASE_PREFIX': 'famir',
        'WORKFLOW_CONNECTION_URL': env.WORKFLOW_CONNECTION_URL,
        //'WORKFLOW_PREFIX': 'bull',
        'EXECUTOR_CONNECTION_URL': env.EXECUTOR_CONNECTION_URL,
        //'EXECUTOR_PREFIX': 'bull',
        //'EXECUTOR_CONCURRENCY': '2',
        //'EXECUTOR_LIMITER_MAX': '1',
        //'EXECUTOR_LIMITER_DURATION': '1000',
      },
      instances: 1,
      kill_timeout: 10_000,
      watch: false
    }
  ]
}
